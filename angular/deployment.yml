apiVersion: apps/v1
kind: Deployment
metadata:
  name: jtq
  namespace:
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jtq
  template:
    metadata:
      labels:
        app: jtq
    spec:
      containers:
        - name: front-end
          image: fe-jtq:latest
          ports:
            - containerPort: 80
